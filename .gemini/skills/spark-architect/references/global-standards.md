# Global Standards - flights-analytics-prod

## Project Constants
- **Project ID**: `flights-analytics-prod`
- **Region**: `europe-west2`

## Naming Conventions
- **Case**: Always use `snake_case` for columns, variables, and file names.
- **Python Style**: Adhere to PEP 8. Use `black` and `isort` for formatting.

## Logging
- Always use the standard `logging` module.
- Log key operations (reading/writing data, API calls, schema enforcement).
- Log exceptions with `exc_info=True`.

## Reference
- **GEMINI.md**: Always adhere to the project's root `GEMINI.md`.
