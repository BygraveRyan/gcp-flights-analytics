# Open Questions

Mirror artifact. This note highlights unresolved repo-level questions and should point back to canonical repo files when answers are decided.

- Should the legacy `.gemini/skills/` set eventually be migrated or deprecated now that `.agents/skills/` is the preferred workspace alias?
- Should the phase table in `README.md` be reconciled with the committed `bigquery/`, `dbt/`, and `dataplex/` assets?
- Is `cloud_functions/ingest_opensky/` still active, or should docs mark it as legacy-only?
